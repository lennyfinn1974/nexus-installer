# ============================================================================
# Nexus Agent — Environment Configuration
# Generated by nexus-installer
# ============================================================================

# Server
HOST=0.0.0.0
PORT=8080
ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

# Database (PostgreSQL)
DATABASE_URL=postgresql+asyncpg://localhost/nexus

# Redis
REDIS_URL=redis://localhost:6379

# Admin Authentication
# This key protects the Admin UI — keep it secret
ADMIN_API_KEY=change-me-to-a-random-secret

# ============================================================================
# Optional API Keys — add these to enable additional features
# All can be configured later via the Admin UI at /admin
# ============================================================================

# Anthropic Claude — cloud AI fallback when Ollama is unavailable
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Brave Search — enables web search capabilities
# Get your key at: https://api.search.brave.com/
BRAVE_API_KEY=

# Mem0 — cloud memory service (optional, local memory works without this)
# Get your key at: https://app.mem0.ai/
MEM0_API_KEY=

# Telegram Bot — enables Telegram messaging integration
# Create a bot via @BotFather on Telegram
TELEGRAM_BOT_TOKEN=

# GitHub — enables GitHub plugin for repo management
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=

# Claude Code — enables agentic Claude Code CLI integration
# Requires claude CLI installed: npm install -g @anthropic-ai/claude-code
CLAUDE_CODE_ENABLED=false
CLAUDE_CODE_CLI_PATH=/opt/homebrew/bin/claude
